Method pipeline figure: sequential stages left-to-right (or top-down for single-column), each stage a rounded box with a short English label, arrows showing data flow direction; stage outputs implied by small glyph shapes between boxes; the paper's contribution stage visually emphasized with the primary hue and slightly larger box; baseline/stage indices as small numerals. Named entities (keep verbatim): present each of the following as a labelled node/block — "Source Enhanced Image", "Residual Response A1", "Residual Response A2", "Connected Component Extraction", "UAPL Pipeline", "WBF Fusion", "CAC-T Thresholding". Core intent — the ONE takeaway a reader must remember: "方法框架流程图：物理引导白盒零样本定位器". Every element serves this message; drop anything decorative that does not. Style: White background, clean publication-grade canvas. Flat editorial rendering: NO drop shadows, NO glow, NO gradient decoration unless explicitly requested; separate elements with crisp borders or whitespace. Restrained semantic palette: 2-4 hues plus neutral greys/black/white; one accent colour on at most 1-2 focal elements; colourblind-safe pairing (never red-vs-green as the only distinction). Exactly THREE visible text tiers — figure title/panel headers (largest), element/series labels (medium), annotations/axis numbers (smallest); keep total text items low (ideally under 15 short labels). All lines and text crisp anti-aliased edges, print-quality, no sketch texture. Keep at least a quarter of the canvas calm (whitespace is part of the design). Avoid: no watermark or logos, no drop shadows, no glow, no gradient decoration, no 3D chart junk, no photographic textures unless asked, no dense text blocks, no decorative clutter, no decorative pictograms or object icons (vegetable sketches, molecule doodads, product silhouettes) unless explicitly asked, no lens flare, no photorealistic branding, no arrowheads pointing backwards unless a genuine feedback loop exists, no isometric/3D boxes. Additional style notes: Default semantic colour mapping unless the user specifies otherwise: primary blue for the proposed method and its emphasis, green family for improvements/gains, warm red family only for baselines or contrast, neutral greys for context and shared structure.
